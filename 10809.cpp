#include <iostream>
#include <string>
#define endl '\n'
using namespace std;

void Init()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

int main()
{
    Init();
    int arr[26] = {
        -1,
    };
    string s;
    cin >> s;

    for (int i = 0; i < s.length(); i++)
        if (arr[i] == -1)
            arr[i] =
                return 0;
}