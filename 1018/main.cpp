#include <iostream>
#define endl '\n'
using namespace std;

void Init()
{
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
}

int main()
{
	Init();
	int m, n;
	cin >> m >> n;
	char arr[51][51];
	for (int i = 0; i < 51; i++)
		for (int j = 0; j < 51; j++)
			cin >> char[i][j];

		return 0;
}